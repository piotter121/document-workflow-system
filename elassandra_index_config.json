{
  "settings": {
    "keyspace": "dws_app"
  },
  "mappings": {
    "version": {
      "discover": "^(?!message|parsed_file_content|file_content|differences).*",
      "properties": {
        "message": {
          "type": "text",
          "cql_collection": "singleton"
        },
        "parsed_file_content": {
          "type": "text",
          "cql_collection": "singleton"
        }
      }
    }
  }
}